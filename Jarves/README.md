# JARVIS — Complete Setup Guide
### Just A Rather Very Intelligent System | Phase 1+2+3+4

---

## ফাইল তালিকা (সব ফাইল একই ফোল্ডারে রাখো):

| ফাইল | কাজ |
|---|---|
| `jarvis.py` | **Main file** — সব Phase একসাথে, এটাই চালাও |
| `jarvis_phase2.py` | Phase 2 extra features module |
| `jarvis_phase3.py` | Phase 3 — Email, Music, Wake Word module |
| `jarvis_phase4.py` | Phase 4 — Auto Voice, 3D Boot Animation module |
| `START_JARVIS.bat` | Double click করলেই সব চালু হবে |
| `README.md` | এই ফাইল |

---

## চালু করার নিয়ম:

### ধাপ ১ — ফোল্ডার বানাও:
```
C:\JARVIS\
```

### ধাপ ২ — সব ফাইল রাখো:
- jarvis.py
- jarvis_phase2.py
- jarvis_phase3.py
- jarvis_phase4.py
- START_JARVIS.bat

### ধাপ ৩ — START_JARVIS.bat তে double click করো

---

## Ollama (Free AI) Setup:
1. https://ollama.com থেকে download করো
2. Install করো
3. CMD তে লেখো: `ollama pull llama3.2`
4. JARVIS চালু করো

---

## Phase 4 নতুন features:
- 🎙 **Auto Voice** — শুধু কথা বলো, কোনো command লাগবে না
- 🔊 **Always Speaks** — সব উত্তর voice-এ বলবে
- 🎬 **3D Boot Animation** — matrix hacker effect
- 👋 **Custom Greeting** — Hello Tanvir Boss, I am Your Assistant JARVIS

---

## সব Commands:

### Phase 1:
`time` `date` `weather dhaka` `news` `network` `wifi` `ping google.com`
`open youtube` `open chrome` `search python` `screenshot`
`note আজকে meeting আছে` `show notes` `remind drink water in 30 min`
`calculate 25*4` `file search resume` `joke` `help` `exit`

### Phase 2:
`clipboard get` `copy hello world` `password 20` `qr code https://google.com`
`translate আমি ভালো আছি` `define hello` `hash mypassword`
`100 USD to BDT` `5 km to mile` `30 C to F`
`world clock` `alarm 07:30` `stopwatch start` `stopwatch stop`
`timer 25 min` `pomodoro` `todo add buy groceries` `todo list`
`process` `kill chrome` `cleanup` `disk analyze` `speedtest`
`organize downloads` `wifi scan` `scan ports` `battery alert 20`

### Phase 3:
`email setup your@gmail.com APP_PASSWORD`
`read email` `send email to x@y.com subject Hi body Hello`
`play music` `pause` `resume` `stop music` `next song` `list songs`
`youtube lofi music` `wiki artificial intelligence`
`stock AAPL` `cricket` `football` `quote` `fact` `briefing`
`continuous voice` `volume up` `volume down` `volume mute`
`export chat` `schedule task morning reminder at 08:00`
`typing test` `pomodoro 25 min`

### Phase 4:
`auto listen on/off` `voice on/off`
(অথবা শুধু কথা বলো — auto-listen সবসময় চালু!)

---

## Email App Password পাওয়ার নিয়ম:
1. Google Account এ যাও
2. Security → 2-Step Verification চালু করো
3. App passwords → Create → নাম দাও "JARVIS"
4. যে password দেবে সেটা ব্যবহার করো

---

*JARVIS by Tanvir Boss | Phase 1+2+3+4 Complete*
